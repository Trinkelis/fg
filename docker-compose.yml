services:
  fg-ffmpeg:
    build: .
    container_name: fg-ffmpeg
    restart: unless-stopped
    ports:
      - "127.0.0.1:7878:3000"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:3000/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3